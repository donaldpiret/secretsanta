<h1>New User</h1>

<%= error_messages_for 'user' %>
<% form_for :user, @user, :url => users_path do |f| %>
	<div>
		<%= render :partial => 'users/form', :object => f %>
		<%= f.submit "Add User" %>
	</div>
<% end %>